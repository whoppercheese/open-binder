# Docker Compose — copied to .env on first deploy (scripts/deploy.sh)
POSTGRES_USER=binder
POSTGRES_PASSWORD=binder
POSTGRES_DB=binder

# Enqueue initial catalog sync when the database has no cards
BOOTSTRAP_CATALOG_SYNC=true
